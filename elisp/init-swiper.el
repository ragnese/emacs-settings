;;; init-swiper.el --- Initializes my Swiper settings. -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:
(use-package swiper
  :ensure nil
  :general (:prefix "SPC" :states 'normal "s" #'swiper))

(provide 'init-swiper)
;;; init-swiper.el ends here

;;; init-dockerfile.el --- Initializes my settings for Dockerfile -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:
(use-package dockerfile-mode
  :mode "\\Dockerfile\\'"
  :ensure t)

(provide 'init-dockerfile)
;;; init-dockerfile.el ends here
